"""PitchLoop agent package (owned by Person 1 / P1).

Contains the autonomous state machine, the deterministic planner and diagnosis,
the generated-tool lifecycle (author + registry), the artifact writer, and the
fake adapters used by P1's end-to-end loop test.
"""
