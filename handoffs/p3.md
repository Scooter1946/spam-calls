READY: no
COMMIT: 97c9959279ecb832b5cc2dd81a6505df612bda78
RUN: bash infra/scripts/start_policy_stack.sh && bash infra/scripts/probe_policy.sh
TEST: pytest -q tests/test_policy_adapter.py tests/test_repo_adapter.py
ENV: PITCHLOOP_RUN_ID, PITCHLOOP_RUN_DIR, PITCHLOOP_ENV_FILE, POLICY_MODE, POMERIUM_ZERO_TOKEN, POMERIUM_IMAGE_TAG, POMERIUM_DENIED_URL, POMERIUM_ALLOWED_URL, POMERIUM_SERVICE_ACCOUNT_TOKEN, REPO_MODE, GITHUB_REPO, GH_TOKEN
OUTPUT: runs/demo-001/policy/deny.json, runs/demo-001/policy/deny.raw.json, runs/demo-001/policy/allow.json, runs/demo-001/policy/allow.raw.json, runs/demo-001/repo/pr.json, runs/demo-001/repo/merge.json
LIVE PROOF: pending at the OUTPUT paths; current local files are fake-mode artifacts and are not represented as sponsor proof
INTEGRATION ENTRYPOINT: integrations.policy_client:build_policy_port; integrations.repo_client:build_repo_port
KNOWN LIMITATION: Pomerium Zero routes and the shared service account must be provisioned in the Zero control plane before the local data plane starts.
BLOCKER: Docker and gh are unavailable on this machine, so the real Pomerium 403/200 pair and GitHub smoke-test PR cannot be captured or honestly marked complete.
